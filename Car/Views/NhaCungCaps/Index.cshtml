@model IEnumerable<Car.Models.NhaCungCap>

@{
    Layout = "~/Views/Layout/AdminLayout.cshtml";
    WebGrid grid = new WebGrid(source: Model, rowsPerPage: 2);
}

<!DOCTYPE html>

@*<html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>Index</title>
    </head>
    <body>
        <p>
            @Html.ActionLink("Create New", "Create")
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TenNCC)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DiaChi)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Email)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.SDT)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.TenNCC)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DiaChi)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SDT)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.MaNCC }) |
                        @Html.ActionLink("Details", "Details", new { id = item.MaNCC }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.MaNCC })

                    </td>
                </tr>
            }

        </table>


    </body>
    </html>*@
<div style="margin:15px">
    <button class="btn btn-success" style="margin:15px"> @Html.ActionLink("Thêm Mới Nhà Cung Cấp", "Create")</button>

    @grid.GetHtml(
        tableStyle: "girdTable",
        headerStyle: "girdHead",
        footerStyle: "girdFooter",
        rowStyle: "girdRow",
        alternatingRowStyle: "girdAltRow",
        columns: grid.Columns(
            grid.Column("TenNCC", "Tên Nhà Cung Cấp"),
            grid.Column("DiaChi", "Địa Chỉ"),
            grid.Column("Email", "Email"),
            grid.Column("SDT", "Số Điện Thoại"),
            grid.Column(format: p => @Html.ActionLink("Chỉnh Sửa", "Edit", new { id = p.MaNCC })),
            grid.Column(format: p => @Html.ActionLink("Xóa", "Delete", new { id = p.MaNCC }))
            ))
</div>
<style>
    .girdTable {
        width: 100%;
        margin: 15px
    }

        .girdTable td {
            border: 1px solid #999999;
            text-align: center;
            width: 100px
        }

    .girdHead {
        background-color: cornflowerblue;
        text-align: center
    }

        .girdHead > th > a {
            color: #ffffff
        }

    .girdFooter {
        font-weight: bold
    }

    .girdRow img {
        width: 370px;
        height: 270px
    }

    .girdAltRow img {
        width: 370px;
        height: 270px
    }

    .girdFooter {
        font-weight: bolder;
        font-size: 15px
    }
</style>

}